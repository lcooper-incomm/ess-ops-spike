import { NumericFormatDirective } from './numeric-format.directive';

describe ( 'NumericFormatDirective', () => {
  it ( 'should create an instance', () => {
    const directive = new NumericFormatDirective ();
    expect ( directive ).toBeTruthy ();
  } );
} );
