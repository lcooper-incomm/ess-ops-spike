import { ClickSwallowerDirective } from './click-swallower.directive';

describe ( 'ClickSwallowerDirective', () => {
  it ( 'should create an instance', () => {
    const directive = new ClickSwallowerDirective ();
    expect ( directive ).toBeTruthy ();
  } );
} );
