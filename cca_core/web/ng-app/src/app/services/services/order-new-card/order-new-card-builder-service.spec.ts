import { OrderNewCardBuilderService } from './order-new-card-builder-service';

describe ( 'OrderNewCardBuilderService', () => {
  it ( 'should create an instance', () => {
    expect ( new OrderNewCardBuilderService () ).toBeTruthy ();
  } );
} );
