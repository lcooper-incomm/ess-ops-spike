export enum AccountType {
  MASTER      = 'Master',
  SUB_ACCOUNT = 'Subaccount',
  RESERVE     = 'SetAside',
  //TODO determine if this is needed when the associated accounts are included
  ASSOCIATED  =  ''
}
